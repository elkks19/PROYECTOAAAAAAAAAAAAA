@model PruebaFinal.Models.Detalle_Wishlist

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Detalle_Wishlist</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="codWishlist" />
            <input type="hidden" asp-for="codProducto" />
            <div class="form-group">
                <label asp-for="fechaAnadido" class="control-label"></label>
                <input asp-for="fechaAnadido" class="form-control" />
                <span asp-validation-for="fechaAnadido" class="text-danger"></span>
            </div>
            <div class="form-group form-check">
                <label class="form-check-label">
                    <input class="form-check-input" asp-for="isCarrito" /> @Html.DisplayNameFor(model => model.isCarrito)
                </label>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
