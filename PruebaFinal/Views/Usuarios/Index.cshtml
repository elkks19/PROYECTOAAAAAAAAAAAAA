@model IEnumerable<PruebaFinal.Models.Usuario>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Persona)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.configUsuario)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Persona.codPersona)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.configUsuario)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.codUsuario">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.codUsuario">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.codUsuario">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
