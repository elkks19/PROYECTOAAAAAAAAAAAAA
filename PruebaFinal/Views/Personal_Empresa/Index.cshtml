@model IEnumerable<PruebaFinal.Models.Personal_Empresa>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Empresa)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Persona)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Empresa.codEmpresa)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Persona.codPersona)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.codPersonalEmpresa">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.codPersonalEmpresa">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.codPersonalEmpresa">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
